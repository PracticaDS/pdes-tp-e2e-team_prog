{
  "name": "pdes-tp-e2e-team_prog",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@babel/node": "^7.4.5",
    "cypress": "^3.3.2",
    "react": "^16.8.6",
    "react-dom": "^16.8.6",
    "react-scripts": "3.0.1",
    "request": "^2.88.0"
  },
  "scripts": {
    "wait": "CYPRESS_baseUrl=http://localhost:81 babel-node wait.js",
    "test:e2e": "CYPRESS_baseUrl=http://localhost:81 cypress run --env API_URL=http://localhost:3001",
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
